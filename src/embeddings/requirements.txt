# Minimal dependencies for embedding generation job
# (excludes torch/transformers which are only needed for local server)
pydantic>=2.0.0
pydantic-settings>=2.0.0
requests>=2.31.0
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
sqlmodel>=0.0.22
pgvector>=0.3.0
